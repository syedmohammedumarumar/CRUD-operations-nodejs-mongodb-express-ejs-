<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>

  </head>
  <body>
    <div class="container">
        <div class="w-full h-full bg-slate-900 p-10 text-white">
            <div class="nav mb-3">
                <a href="/read" class="text-2xl text-blue-500 ">Read users</a>
            </div>
            <h1 class="tracking-tighter text-3xl mb-4">Edit user</h1>
            <form  action="/update/<%= user._id %>" method="post">
                <input class="px-5 py-3 bg-transparent border-2 border-zinc-600 rounded-lg outline-none" type="text" name="name"  placeholder="name" value="<%= user.name%>">
                <input class="px-5 py-3 bg-transparent border-2 border-zinc-600 rounded-lg outline-none" type="text" name="email"  placeholder="email"value="<%= user.email%>">
                <input class="px-5 py-3 bg-transparent border-2 border-zinc-600 rounded-lg outline-none" type="text" name="image"  placeholder="image url" value="<%= user.image%>">
                <input class="px-4 py-3 bg-yellow-400 text-black rounded-lg" type="submit" name="" value="Update user">
            </form>
        </div>
    </div>
  </body>
</html>
